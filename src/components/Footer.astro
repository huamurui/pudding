---
const footerLinks = {
  social: [
    { name: "GitHub", href: "https://github.com/huamurui" },
    { name: "Email", href: "huamurui@outlook.com" },
    { name: "RSS", href: "/rss.xml" },
    { name: "Sitemap", href: "/sitemap-index.xml" },
  ],
};

const currentYear = new Date().getFullYear();
---

<footer class="footer">
  <div class="footer-container">
    <!-- 链接 -->
    <div class="footer-links">
      {
        footerLinks.social.map((link) => (
          <a
            href={link.href}
            class="nav-link footer-link"
            aria-label={link.name}
            target="_blank"
            rel="noopener noreferrer"
          >
            <span>{link.name}</span>
          </a>
        ))
      }
    </div>

    <!-- 底部版权信息 -->
    <div class="footer-bottom">
      <p class="footer-description">
        A blog about programming tech and nonsense.
      </p>
      <p class="copyright">
        &copy; {currentYear} huamurui. All rights reserved.
      </p>
      <p class="credit">
        Powered by <a
          href="https://astro.build/"
          class="nav-link footer-link"
          target="_blank"
          rel="noopener noreferrer">Astro</a
        >.
      </p>
    </div>
  </div>
</footer>

<style>
  /* 基础样式：无背景，弱化分割 */
  .footer {
    margin-top: 1rem;
  }

  /* 容器：限制宽度+居中 */
  .footer-container {
    border-top: 1px dashed var(--border-color);
    max-width: 800px; /* 按要求限制最大宽度 */
    margin: 0 auto;
    padding: 1rem;
    text-align: center; /* 整体文字居中 */
  }

  .footer-description {
    line-height: 1.5;
    max-width: 600px;
    margin: 0.5rem auto;
  }

  .footer-links {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    justify-content: center;
    gap: 1.5rem;
  }

  .footer-link {
    text-decoration: none;
    transition: color 0.2s ease;
    text-decoration: underline;
  }

  /* 底部版权信息 */
  .footer-bottom {
    /* padding-top: 1rem; */
    /* border-top: 1px dashed var(--border-color); */
     /* 虚线分割，弱化视觉效果 */
  }

  .copyright {
    margin: 0 0 0.5rem 0;
  }

  .credit {
    margin: 0;
  }
  /* 响应式调整 */
  @media (max-width: 600px) {
    .footer-links {
      flex-direction: column;
      gap: 0.75rem;
    }

    .footer-bottom {
      font-size: 0.8rem;
    }
  }

  /* 减少运动偏好 */
  @media (prefers-reduced-motion: reduce) {
    .social-link {
      transition: none;
    }

    .social-link:hover {
      transform: none;
    }
  }
</style>
